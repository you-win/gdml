<script>
extends Node2D

func _ready():
    Physics2DServer.area_set_param(get_world_2d().space, Physics2DServer.AREA_PARAM_GRAVITY_VECTOR, Vector2.ZERO)
    queue_free()
</script>

<canvas_layer>
    <gdml style="anchor_left: 0.4; anchor_right: 0.6; anchor_bottom: 0.9;margin_top: 10">
        <label style="align: (int) 1">
            hello
        </label>
    </gdml>
</canvas_layer>

<camera_2_d props="current: (bool)true"/>
<kinematic_body_2_d name="LeftPaddle" props="position:x: (int)-200">
    <script>
extends KinematicBody2D

var test_var = 1

func _ready():
    var cs = CollisionShape2D.new()
    var rect = RectangleShape2D.new()
    rect.extents = Vector2(32, 32)
    cs.shape = rect
    add_child(cs)

    var sprite = Sprite.new()
    var image = Image.new()
    <!-- TODO make this portable aka not a res path -->
    image.load("res://icon.png")
    var image_texture = ImageTexture.new()
    image_texture.create_from_image(image)
    sprite.texture = image_texture
    add_child(sprite)

    scale.y = 3

func _physics_process(delta):
    if Input.is_key_pressed(KEY_W):
        position.y -= 10
    if Input.is_key_pressed(KEY_S):
        position.y += 10

    </script>
</kinematic_body_2_d>
<kinematic_body_2_d name="RightPaddle" props="position:x: (int) 200">
    <script>
extends KinematicBody2D

func _ready():
    var cs = CollisionShape2D.new()
    var rect = RectangleShape2D.new()
    rect.extents = Vector2(32, 32)
    cs.shape = rect
    add_child(cs)

    var sprite = Sprite.new()
    var image = Image.new()
    image.load("res://icon.png")
    var image_texture = ImageTexture.new()
    image_texture.create_from_image(image)
    sprite.texture = image_texture
    add_child(sprite)

    scale.y = 3

func _physics_process(delta):
    if Input.is_key_pressed(KEY_UP):
        position.y -= 10
    if Input.is_key_pressed(KEY_DOWN):
        position.y += 10

    </script>
</kinematic_body_2_d>
<rigid_body_2_d name="Ball">
    <script>
extends RigidBody2D

func _ready():
    var cs = CollisionShape2D.new()
    var rect = RectangleShape2D.new()
    rect.extents = Vector2(32, 32)
    cs.shape = rect
    add_child(cs)

    var pm = PhysicsMaterial.new()
    pm.friction = 0.0
    pm.bounce = 1.0

    physics_material_override = pm

    var sprite = Sprite.new()
    var image = Image.new()
    image.load("res://icon.png")
    var image_texture = ImageTexture.new()
    image_texture.create_from_image(image)
    sprite.texture = image_texture
    add_child(sprite)

    apply_central_impulse(Vector2(-200, 0))

    </script>
</rigid_body_2_d>

